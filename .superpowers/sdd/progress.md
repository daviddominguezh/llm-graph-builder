# Runtime Unification (RU1–RU6) SDD progress

Branch: feat/migration (single long-lived branch for all of RU1–6)
Mode: subagent-driven, commit-per-task, RU1 ∥ RU2 in parallel in shared tree.
Model: ALL subagents = opus (user standing rule).
Base of branch: 248894c8 (merge of data-tools into main).

## Standing rules for every implementer dispatch
- NEVER discard/revert a change you did not author. If a file you must touch
  already has an unexpected modification, preserve it and integrate around it;
  if it blocks you, STOP and report — do not delete it. (Shared parallel tree.)
- Only modify files in your declared scope. Commit only your own task's files
  (stage explicitly; never `git add -A`/`-am`).
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **LINT GATE (lesson):** EVERY implementer AND fix dispatch must run `npm run lint -w packages/<pkg>` (0 errors) in its scoped checks — not just tests+typecheck. The RU2-T2 *fix* dispatch skipped lint and committed 2 test-lint errors (max-lines describe + negated condition); caught by RU2-T3 agent, fixed in commit 4c36af9c. Backend lint now EXIT 0.
- **USER COMMITS IN PARALLEL:** the user pushes their own commits onto feat/migration while we work (e.g. 240c3342 "updated build command", 7169916e "better ui"). ALWAYS derive each task's review-package BASE from the ACTUAL git parent (`git log`/`git rev-parse`), never assume it's the previous RU task's SHA — otherwise the review diff sweeps in the user's commit. Commit with explicit pathspec so their in-flight changes are never staged. If `review-package BASE HEAD` reports >1 commit for a single task, the BASE is wrong — recompute from `git log`.
- **FULL-SUITE GATE (lesson):** a task's verification must run the FULL package test suite (`npm run test -w packages/<pkg>`), NOT a narrow `--testPathPatterns=X` — a filtered run masks SIBLING-test regressions in the same package. RU1-T13 (re2→re2js) changed an error message and broke a pre-existing `filter.test.ts` assertion, masked because the agent only ran `--testPathPatterns=matcher`. The reviewer caught it by running the whole suite. Reviewers/controller: run the full package suite at task close.

## Shared-file collision points (serialize these task pairs)
- root `package.json` / `tsconfig.json` — RU1-T1 only (RU2 T1 disjoint).
- `packages/backend/src/routes/internal/internalRouter.ts` — RU1-T7 (/rerank) ↔ RU2-T8/T9 (/mcp/*). SERIALIZE.
- `packages/backend/src/server.ts` allowlist — RU1-T7 ↔ RU2-T8/9 possible. Watch.
- `packages/backend/package.json` — RU1-T11 (add dep) ↔ any RU2 dep add. Watch.

## RU1 — shared-store-services (13 tasks)
Task 1: complete (commit 33f1728d, parent e004ea70, review Approved/clean). Scaffold + opaque-cursor codec; full check EXIT 0.
  CARRY-FORWARD: new package's `references: ../api` was DROPPED (TS6310: api sets noEmit while composite). The first later RU1 task that couples shared-store-services to api types (likely T2 SearchPage in api, or T11 shims) must re-add the reference AND resolve api's tsconfig noEmit/composite conflict (touches packages/api/tsconfig.json — outside that task's nominal file set; budget for it).
  Minor (final review): decodeCursor<P extends object> narrower than mandated <P> (forecloses primitive payloads); Buffer used vs web-standard btoa/Uint8Array (Workers/Deno provide shims, acceptable); non-object-JSON throw branch (pagination.ts:46-48) untested. @supabase/supabase-js pinned ^2.99.0 (monorepo-consistent — sound deviation from brief's ^2.45.0).
Task 2: complete (commit 5590019e, parent 33f1728d, review Approved/clean). api search contract offset→cursor; total/truncated dropped; SearchPage<T> declared LOCALLY in api (structurally identical to package primitive — transitional duplicate the plan accepts; api↔package dep/reference STILL deferred). Contract test non-vacuous (@ts-expect-error load-bearing).
  ⚠️ EXPECTED-RED WINDOW: `npm run typecheck -w packages/api` is intentionally RED on EXACTLY 4 deferred consumer files (providers/kv_store/buildTools.ts, providers/rag/buildTools.ts, + their 2 tests) from T2 until RU1-T10. Full `npm run check` will NOT be green until T10. When running full-tsc before T10, the red set MUST be exactly these 4 — any other red = a real regression to investigate. RU2 stays verifiable via jest (ts-jest per-file tolerates this).
  ⚠️ BACKEND ALSO RED until RU1-T11: RU1-T2's api contract change ALSO breaks the EXISTING backend store services that consume the old api types. Confirmed CONFINED set (after `cd packages/api && npx tsc -p tsconfig.build.json` regenerates dist/.d.ts): backend red = ONLY `services/kvStoreService.{ts,test}` + `services/ragStoreService.{ts,test}` (→ become shims at RU1-T11). FULL expected-red set across branch until RU1 done = api{kv_store/buildTools,rag/buildTools +2 tests}(→T10) + backend{kvStoreService,ragStoreService +2 tests}(→T11). 8 files.
  ⚠️ KEEP api dist PRESENT: backend resolves @daviddh/llm-graph-runner via api's built dist/.d.ts. `npm run build`/`build:api` does `rm -rf dist` then fails (api red) → leaves NO dist → backend can't resolve api → CASCADE of "cannot be resolved" lint/type errors everywhere. Fix: regenerate api dist with `cd packages/api && npx tsc -p tsconfig.build.json` (emits .d.ts despite type errors). After that, backend red is confined to the 4 files above. RU2 task gate = jest + file-scoped `npx eslint <file>` (package-wide backend typecheck is fine to use IF you filter the 4 known-pending files).
Task 3: complete (commit 79d87b50, parent 2c354c99, review Approved/clean). kvQueries.ts — keyset list/get-by-keys/ILIKE-prefilter/raw-scan; ILIKE escapes \ % _ (order-safe); generic self-type helpers (applySeek/applyIlikeTarget). Test uses isSupabaseClient type-guard (sanctioned, no unsafe cast). Lint 0, typecheck clean, 4/4.
  🔒 SECURITY CARRY-FORWARD → T4/T5 (MUST address): for on:'both', escapeLikePattern escapes LIKE metachars but NOT PostgREST `or()` structural chars (`,` `.` `(` `)`). LLM-supplied term/literal interpolated into `key.ilike.${p},value.ilike.${p}` (kvQueries.ts:241) could alter filter structure. Whichever task wires LLM input into kvQueries must constrain/escape PostgREST metacharacters (or avoid the interpolated `or` string). No callers yet → in-spec for T3.
  Minor (final review): test comment falsely cites "copied from executeFetcher.test.ts" (that file is in backend, not this package) — fix comment; getByKeys/upsertValue have zero test coverage (stub lacks .in()/.upsert()) — cover when T5 factory exercises them.
Task 4: complete (commit fbb76ff3 + fix 5f3a9a78, parent chain from 0b5ac5fa, review Approved after 1 fix loop — Critical found+fixed). regexSearch.ts: RE2JS.compile (ReDoS-safe .test), extractRequiredLiteral + skipCharClass + pure matchEntry. CRITICAL soundness bug: extractRequiredLiteral emits an optional/starred GROUP's interior as "required" ((abc)?→"abc", (ab)*→"ab", x(ab)*y→"ab", (abc)*def→"abc", (hello)?world→"hello") because it only checks quantifier-after-literal-char, never quantifier-after-`)`. As an ILIKE prefilter this DROPS real matches (data loss). Fix: a quantifier in {?, *, {0,..}} after `)` makes the group non-required → discard that group's run, keep other required literals (e.g. (abc)*def→"def"). +RED tests for the table. Re-review verified SOUND across nested groups/ranges/escaped-parens/char-class/alternation. 9/9, lint 0. (Minor for final: escaped metachar \.com→"com" under-prefilters — sound but less selective.) 🔒 T5 carry-forward unchanged: literals may contain PostgREST chars — T5 must escape at or()/ilike wiring.
Task 5: INTERRUPTED mid-run by session restart (agent killed, no report) — left partial BROKEN work in tree (unfinished 141-line fn, test failing typecheck/lint). RE-DISPATCHED to COMPLETE (preserve task's own output, not discard). Files: kvStoreService.ts + test (new), kvQueries.ts + test + index.ts (modified, injection fix). 🔒 MUST close PostgREST or() injection with proof test. COMPLETE (commit cee80f57 + fix cf43ebbf, review Approved after 1 fix loop): injection closed (quotePostgrestValue, proven non-vacuous); FIX regex tail-drop on limit-interrupted final page (ingestBatch {acc,limitReached}; null only when exhausted&&!limitReached); 19 tests, full SSS 29/29. Minor(final): cursor guard isKeysetCursor for both keyset+regex; decodeCursor fail→generic Error not ToolError. KEEP api dist alive for backend (ToolError runtime import): if npm run build wipes it, regen `cd packages/api && npx tsc -p tsconfig.build.json; npx tsc-alias -p tsconfig.build.json`.
Task 6: complete (commit 68d39150, parent abdd6ea6, review Approved/clean). internalApiClient.ts (portable fetch: POST /internal/embed + /internal/rerank, injected baseURL+masterKey, Node/Workers/Deno) + rag/ragQueries.ts (rag_text_search/rag_semantic_search RPC wrappers, keyset + bounded pool). NO injection path (parameterized RPCs, no .or()/.ilike() string-building). 37/37, lint 0.
Task 7: committed c41b6804 (parent ea729089) — complete (review Approved/clean). Carry-forward: native `re2` still imported in backend rag/search/regex.ts + services/kvStoreService.ts (remove at T11 shim / T13 re2→re2js). /internal/rerank (handleRerank: {query,records:[{id,content}],topN}+x-master-key → {records:[{id,score}]}, reported to match T6 client) + DROP /internal/regex/validate (handleRegexValidate + native RE2 + PATTERN_MAX removed; re2js in-process). /internal/mcp/* (RU2-T8) untouched. Also touched testHarness.ts (BodyShape records?) + authGate.test.ts (repointed dead path). 10/10.
Task 8: committed 7e72e9e1 (parent c41b6804) — IN REVIEW. rerank.ts (rerankPool always-on via T6 client) + ragStoreService.ts (bm25/semantic/hybrid/regex → embed→retrieve bounded pool→rerank→RagPoolCursor paginate). No rerank toggle. Cursor round-trip tested (no drop/dup, exhaustion→null) — reviewer scrutinizing vs the T5 KV drop bug (rerank-once-over-pool vs per-page). COMPLETE (commit 7e72e9e1, review Approved/clean): CONFIRMED no KV-style drop bug (paginate uses slice.length not start+limit; round-trip test real). 6 tests (report wrongly said 10), lint 0.
  ⚠️ SPEC-LEVEL CAVEAT — USER DECISION at RU1 integration (NOT a T8 defect, matches the brief's pseudocode): RagPoolCursor is STATELESS ({poolIndex} only). On resume the WHOLE pipeline re-runs (re-fetch pool RPC + re-embed for semantic/hybrid + re-rerank) then slices at poolIndex. Cross-page no-drop/no-dup depends on retrieval+rerank order being byte-identical across separate calls — NOT guaranteed in prod (rag_text_search/rag_semantic_search tie-breaking has no stable secondary sort; rerank nondeterminism) → could silently drop/dup across pages. Also re-embeds+re-reranks per page (cost). Options: (a) accept as-spec; (b) stash reranked id-order/pool-hash in the cursor; (c) add stable secondary sort (by id) to the RPCs + rerank determinism. RAISED WITH USER.
Task 9: complete (commit 38ecbf36, parent f36388c0, review Approved/clean). leadScoringService.ts (get + read-merge-write set on conversations.metadata.lead_score — PRESERVES siblings like forms, tested capturing the literal UPDATE patch; no RPC/migration) + formsService.ts/formsQueries.ts (port of web formsQueries, supabase injected). OutputSchemaField derived from FormDefinition['schemaFields'][number] (no graph-types dep added). null-metadata handled. 4 tests, full SSS 47/47.
  Minor (final): forms helpers untested (queryFormsForAgent join+mapping + applyFormFieldsAtomicQuery carry real logic, 0 coverage — mechanical port of working web code); read-merge-write has inherent last-writer-wins race (acceptable, matches edge being ported).
Task 10: complete (commit 4949e487, parent caba2832, review Approved/clean). 🎯 MILESTONE: api typecheck 16→0 → packages/api GREEN. KV+RAG buildTools/descriptors/descriptions: offset→cursor (input+JSON-schema), result {items,limit,nextCursor} (drop total/truncated/offset copy), CURSOR_DESC/NEXT_CURSOR_DESC. All KV+RAG modes threaded. 38 tests. (Brief's offset-rejects test omitted — Zod strips unknown keys so it could never throw; correctly skipped.) Minor(final): RAG lacks a positive cursor-threading assertion (KV has cursor:'abc'); some NEXT_CURSOR/LIMIT descs exported-unused (for future response schemas).
Task 11: COMPLETE (commit aeed77b8, review Approved/clean). 🎯 MILESTONE: backend typecheck 22→0 → packages/backend GREEN (api + backend BOTH green now). services/{kv,rag}StoreService.ts → shims over @daviddh/shared-store-services (ragStoreService env-bound, BACKEND_INTERNAL_URL documented); searchChunks drops rerank toggle (always-on); edge execute-agent kv/rag/internalApiClient re-exports (rerank client+, validateRegexPattern−). re2 KEPT (rag/search/regex.ts + utilityHandlers.ts still use it → RU1-T13). 20 tests.
  Carry-forward (edge runtime, to-be-deleted RU4/RU6 — NOT breaking backend typecheck): edge toolBuilder.ts calls makeRagStoreService with 2 args (needs 3rd client); ragSearchCores.ts orphaned. Remaining RU1: T12 (FE rerank-toggle removal + i18n), T13 (re2→re2js in shared-validation + remove re2). Remaining RU2: T12 (sim wiring — done, in review). membership comma-parse FIX done (56e9bd77, Approved).
Task 12: complete (commit 032e92d6, review Approved/clean). FE RAG rerank toggle removed (RagSearchBar/RagTenantContent/ragFiles SearchOptions+search + en.json rerankLabel/rerankTooltip removed + description.hybrid reworded). Grep-verified no orphaned rerank ids (rerank_score response field + tools-extract translation string correctly kept). Only en.json locale (i18n complete). Typecheck/lint clean.
Task 13: COMPLETE (commit 92d8b35a + fix cd58a791, review Approved after 1 fix loop). Native re2 ELIMINATED repo-wide (matcher.ts + rag/search/regex.ts → re2js; re2 removed from shared-validation + backend package.json → whole monorepo native-addon-free for regex, Workers/Deno-portable). matcher flag-string→bitwise (i→CASE_INSENSITIVE), RE2JSSyntaxException wrapped 'invalid regex:'. FIX: re2js error wording broke pre-existing filter.test.ts (masked by --testPathPatterns=matcher) → assertion now engine-agnostic /invalid regex/; full shared-validation suite 50/50. ReDoS test load-bearing. Minor(final): dormant flag change (m/s ignored, unknown flags no longer throw — unreachable, no prod regex-matcher caller); error-prefix inconsistency vs regexSearch (invalid regex: vs invalid_pattern:).

*** RU1 (SHARED-STORE-SERVICES) COMPLETE: 13/13 + fix-loops (T4 extractor soundness, T5 pagination+injection, T13 filter test) — all reviewed clean. ***
*** WHOLE-BRANCH `npm run check` = EXIT 0 (green across ALL packages). RU1 ∥ RU2 FOUNDATION COMPLETE — branch ready for RU3 (which depends on both). ***

## FINAL WHOLE-BRANCH REVIEW (248894c8..cd58a791, 35 commits) — Foundation MERGE-READY: YES
All 6 cross-cutting seams PASS (poolKey identity, McpPoolClient↔/internal/mcp/invoke contract, SearchPage/cursor consistency, internalApiClient↔/internal/rerank, backend/edge shims, security all-5-chains). No must-fix-before-this-merge. All gaps are inside declared RU3/RU4/RU6 deferrals, cleanly bounded.

### 🚨 MUST-FIX-BEFORE-THE-POOL-GOES-LIVE (RU3/RU4 scope — wire these in the SAME change that injects mcpPool into the live ProviderCtx path):
1. **Start keepalive + eviction timers in server bootstrap.** Today they're defined in pool/ but never started, yet buildInvokeDeps sets `isHealthy:()=>true` ("liveness maintained out of band by keepalive"). If the pool is activated WITHOUT keepalive+eviction → dead warm handles pass isHealthy → skip reconnect → callTool fails → FALSE uncertain_outcome; + unbounded entry growth (no eviction). 
2. **Wire consistent-hash routing into the handlers.** ring.ts/routing.ts/membership.ts (decideRoute/forwardWithReroute/resolveMembers/buildRing) are referenced ONLY by each other + tests — never by mcpInvoke.ts/mcpPreflight.ts/server.ts. Stickiness currently rests ENTIRELY on fly.toml replay_cache (pin by x-mcp-poolkey). Either wire ring↔replay_cache + decide what preflight routes on (it has no api caller sending x-mcp-poolkey), OR formally decide replay_cache is the sole mechanism and DELETE the unused ring/routing/membership.
3. **Fix edge makeRagStoreService 3-arg re-export** (edge toolBuilder.ts 2-arg call → client=undefined → crash on client.embed) before any edge RAG path runs — or complete the edge deletion (RU6).
### Minor — ITEMS 4 + 5 FIXED (commit 5b562e38: ragStoreService :4000 default + McpPoolClient binding-category mapping, both RED→GREEN tested). Item 6 (SearchPage DRY) remains, unify when convenient.
4. [FIXED 5b562e38] backend services/ragStoreService.ts defaults baseUrl to http://127.0.0.1:3001 but backend listens on 4000 (masterKey defaults ''); works when BACKEND_INTERNAL_URL set (it is in .env.example) but wrong fallback → silent embed/rerank failure if unset. Default to :4000 or make required. (Also: backend RAG search now does a localhost HTTP round-trip to itself for embed+rerank — intended portability tradeoff.)
5. McpPoolClient.parseResponse throws transport on any !res.ok, never reads body.category → 'binding' category unreachable api-side (collapses to transport). Harmless (both pre-exec/retryable) but a contract loss, untested — add a 400-binding case.
6. SearchPage<T> defined twice (api types.ts + package pagination.ts) — DRY debt (structurally identical, assignable). Unify when convenient.
### Deferral coherence: ALL verified cleanly bounded (sim adapter dead-until-wired, mcpPool prod injection + McpInvoker root-export absent as expected, poolKeyShared dup noted, edge orphans RU4/RU6, connectFailedUntil cooldown unread, RagPoolCursor stateless-per-page = user decision pending).

# ===== RU3 (RUNTIME CORE + SIMULATION) — 21 tasks, tightly coupled (chain) =====
Plan: docs/superpowers/plans/2026-06-24-RU3-runtime-core-simulation.md. Same standing rules as RU1/RU2 (controller commits explicit-pathspec, ALL subagents opus, per-task lint gate + FULL package test at close, NO `npm run check` [user rule], review-package BASE from real git parent [user commits in parallel], never discard user's uncommitted web work).
RU1/RU2 cross-deps CONFIRMED: RU2 exports createMcpPoolClient+McpPoolError from providers/mcp/index.ts; McpInvoker interface at providers/mcp/poolClient.ts:14 but NOT re-exported (RU3 Task 4 uses the real one + ADD the root export — this is the final-review must-fix + RU3's pool-wiring). RU1 = shared-store-services (sim noops builtins via simulatedNoop seam Task 12, so RU3 sim barely touches real stores). RU3 IS where RuntimeServices.mcpPool gets wired live → must ALSO start keepalive+eviction timers + wire routing (the must-fix-before-pool-goes-live items) when it injects the pool.
Dependency structure: T1(events/types) ∥ T3(runtime/types) ∥ T7(jsonPointer) independent foundations → T2←T1, T4←T3, T5←T3/4, T6←T4, T8←T7. T9(ProviderCtx→env-union) = BREAKING cross-cutting (updates provider.ts + ALL builtins + construction sites in one task, like RU1-T2 contract-before-consumers). T14(StepMachine)←T1-13, T15(executeTurn)←14, T17-20(sim+FE wiring)←15, T21 full gate.
Tasks:
T1: complete (commit af037ebe, review Approved/clean). events/types.ts = 14-member ExecutionEvent superset union + Tokens (interface, love consistent-type-definitions); runtime/childResult.ts = ChildResult/ChildErrorCode TYPE stub (child_finished arm references it → Task 5 APPENDS mapTerminationToChildResult to this SAME file, don't recreate). 3/3 test.
T3: complete (commit 9063347e, review Approved/clean). runtime/types.ts: ExecutionType, DeepReadonly (Array<infer U>), RuntimeInput/Output env unions (production/simulation arms keyed on `environment`), MAX_DISPATCH_DEPTH=3 (canonical, RU4 consolidates), MAX_CHILD_RUNTIME_MS=3_600_000. Minor→T9: DeepReadonly maps function types to {} (only wraps Record<string,unknown> now = unaffected; add a comment if reused on ProviderCtx which has methods).
T7: complete (commit 437b2577, review Approved/clean — RFC 6901 unescape order VERIFIED, immutable, 100% cov). Minor(final): add ~01→~1 order-trap test (impl correct but a replaceAll-order flip would pass silently); non-/-prefixed pointer silently no-ops (T8 caller: malformed pointers fail silent, not loud).
T2: complete (commit 121e53f7, review Approved/clean — 6 concurrency props traced: no-lost/FIFO/close/emit-after-close-noop/no-double-resolve; Promise.withResolvers OK on ES2024/Node24). Minor(final): emit-after-close guard untested (91.66% branch cov, behavior correct — add 1-line test); package.json engines.node>=18 inaccurate for Promise.withResolvers (Node22+, pre-existing repo-wide).
T5: complete (commit ff6806c2, review Approved/clean). mapTerminationToChildResult appended to childResult.ts — §6.4 env-aware (sim END→awaiting_input; prod END→finished/no_result), precedence depthExceeded>failure>finish>END verified, all 6 ChildErrorCodes covered, 12 tests. Minor(final): 3 failure tests assert code inside if(status==='error') w/o a preceding status assert (vacuity gap); no failure>finish precedence test.
T4: complete (commit de9a1e53, review Approved/clean). capabilities/ contracts: DispatchPersistence/Strategy/Observability + RuntimeCapabilities/RuntimeServices (mcpPool=REAL McpInvoker, resolveChildConfig, supabase=SupabaseLike) + ResolvedChildConfig (skills: real SkillDefinition[], matches backend simulateChildResolver field-for-field). McpInvoker already re-exported from providers/mcp/index.ts. Minor(final): SupabaseLike=Record<string,unknown> loose — narrow to from/rpc later.
T8: complete (commit 5dc96804, review Approved/clean). simStateStore deep-freeze read / clone-write via setByJsonPointer / patch list / snapshot — all 5 immutability guarantees verified. abort() genuinely absent from brief (title wording only). Minor→T19: patch.value shares a ref with internal state (patches() copies the array but not each patch.value → a consumer mutating patches()[i].value could corrupt live state; read/snapshot safe). One brief-prescribed `as DeepReadonly` (runtime-sound widening of a frozen value).
T6: complete (commit 8d11b760, review Approved/clean). sim caps: noopPersistence + consoleObservability/consoleLogger (routed through utils/logger for no-console) + noopRateLimit (always-allow). 7/7.

*** RU3 FOUNDATION (T1–T8) COMPLETE — all reviewed clean, ~17 commits, api green throughout. ***
MIDDLE LAYER: T9 (ProviderCtx→env-union) IN PROGRESS — 4 files: provider.ts union + runtime/simulatedNoop.ts + providerCtxFromContext (prod arm) + backend simulationProviderCtx (sim arm). COMPLETE (commit eb22c1c1, review Approved — union sound: narrowing correct, sim unambiguous/non-forgeable, DeepReadonly off logger/services). Union is a proper SUPERSET so builtins stay compiling; api+backend typecheck both 0 PASSED.
  🚩 CARRY-FORWARD → T12/T15 (design debt, not bugs): (a) dispatchDepth shipped OPTIONAL (brief said required) — consumers `?? 0`; tighten to required once all sites thread depth; (b) buildCatalogProviderCtx (backend getRegistry.ts:38) is a REAL production ctx NOT updated — set environment:'production'+dispatchDepth explicitly there; (c) T12 per-builtin guard MUST use NEGATIVE-DEFAULT (`=== 'simulation'` else production), never a positive prod test (env-less prod ctxs would misclassify); (d) Minor: conversationId left in base (structurally present on sim arm too, benign) — should live on prod arm only.
T10: complete (commit 19ed7cbe, review Approved/clean). syncRecurseStrategy — depth guard dispatchDepth+1>MAX(3): depths 0/1/2 run, 3+ blocks→completed w/ max_depth_exceeded ChildResult (runChild NOT called when blocked); always completed inline (never suspends). Minor(final): test const AT_LIMIT_DEPTH naming cosmetic.
IN PROGRESS (parallel): T11 (childDispatch ←T2/T5/T10), T12 (per-builtin simulatedNoop guard ←T9 — NEGATIVE-DEFAULT `=== 'simulation'` else prod, 5 builtins), T13 (resolveChildConfig adapter ←T4). Then T14 StepMachine (Agent+Workflow adapters + selectStepMachine) → T15 executeTurn → T16 barrel+completeness → T17-20 sim/FE cutover → T21 gate.
T13: complete (commit 254c4572, review Approved/clean). makeResolveChildConfig(supabase, backendResolve) closure — forwards supabase+input to the injected backend resolver; injection seam preserved (no backend import), conforms RuntimeServices.resolveChildConfig exactly. 2/2.
T11: complete (commit 3a4ba436, review Approved/clean). childDispatch — emits child_dispatched → terminal (child_finished/child_awaiting_input/child_suspended, all real union members), runChild maps via mapTerminationToChildResult, strategy owns the depth short-circuit, returns ChildResult for re-injection. 4/4, 100% cov. (child_failed = a ChildErrorCode carried in ChildResult, NOT an event — report naming slip.)
T12: complete (commit 53b2496e, review Approved/clean). All 5 builtins guarded via a SINGLE withSimGuard chokepoint at the tool-SELECTION layer (leak-resistant — future tools auto-guarded) — negative-default `=== 'simulation'`, before real effect. sim=marker+service-not-called, prod=real path (probed per underlying service method). NO real-infra leak path. Minor(final): only 1 representative tool tested per builtin (siblings covered by chokepoint construction); lead_scoring withSimGuard used-before-declared (hoisting-legal).

*** RU3 MIDDLE LAYER (T9–T13) COMPLETE — 13/21 done. ***
T14: complete (commit d9d22d3b, review Approved — core correct/typed/tested, DI verified vs real engines). StepReport union (step/dispatch/awaiting_input/terminal); AgentStepMachine/WorkflowStepMachine wrap INJECTED runAgentLoop/runWorkflow closures; advance() checks dispatchResult FIRST else terminal (workflow null→terminal ''); selectStepMachine by type. Dropped brief's unused `pending` (re-injection via onChildResult + driver-owned msg array; RU4 reintroduces).
  🚩 CARRY-FORWARD → T15/T17 (integration): (1) selectStepMachine uses optional-closures + runtime-throw (loses compile-time safety) — ADOPT function OVERLOADS [selectStepMachine('agent', AgentMachineDeps):StepMachine; ('workflow', WorkflowMachineDeps)] for call-site safety, keep the throws as defense. (2) assistant_message DOUBLE-EMIT: agentStepMachine.advance() ALWAYS emits assistant_message(finalText); workflow adapter does NOT (text via onNodeProcessed). T15/T17 wiring runAgentLoop's onStepProcessed MUST NOT ALSO emit the final assistant_message → resolve OWNERSHIP (adapter OR closure, not both). (3) awaiting_input/step StepReport variants are NEVER produced by adapters (CORRECT — adapter can't know env); the DRIVER (T15) materializes awaiting_input from terminal.finishResult via mapTerminationToChildResult (finishResult absent + sim → awaiting_input).
T15: complete (commit af9d5bb0, review Approved — driver correct, STACK-SAFE [the await trampolines the recursion — opposite of the flagged risk], event ownership + awaiting materialization correct). advance→dispatch(childDispatch→inject→continue)/terminal(finalize); assistant_message adapter-only; awaiting via mapTerminationToChildResult; RuntimeOutput env-discriminated; shared emitter. 6/6.
  🚩 CARRY-FORWARD → T17/T18: (IMPORTANT) childExecutionId COLLIDES across SEQUENTIAL dispatches in one turn — childIdFor='child-${dispatchDepth+1}' constant per executeTurn call → N sequential children share one id → breaks dispatched↔finished correlation. FIX when plumbing real child execution IDs (T17/T18) OR add per-dispatch counter suffix child-${depth}-${n} before multi-dispatch ships. Minors: add exhaustiveness `never` guard on step fall-through; step rich-fields dropped (confirm T17/T18 adapters EMIT them); simStore-snapshot + child error/awaiting re-inject paths untested; orgId:'' placeholder (T17/T18 plumbs real org).

*** RU3 RUNTIME CORE (T1–T15) COMPLETE — 15/21, ~19 commits, api green throughout. ***
CUTOVER PHASE remaining: T16 (public barrel exports + emitterCompleteness §5 assertion) → T17 (backend sim caps wiring + ExecutionEvent→sim-SSE bridge, both engines) → T18 (migrate BOTH sim handlers onto executeTurn behind ONE handler, BE routes by appType; also: real child IDs, selectStepMachine call-site+overloads, orgId plumbing) → T19 (FE sim-state store hook + JSON panel + MCP badge + dialogs) → T20 (collapse 2 FE stream fns into one + translations + toolbar) → T21 (full gate). T17/T18=backend; T19/T20=web.
T16: complete (commit c88021f1, review Approved/clean). Barrel additive (161 ins, 0 del) — full RU3 public surface re-exported, no existing exports touched, no duplicates, all resolve. emitterCompleteness §5: 3 consumer shapes built as ExecutionEvent (compile-time superset guard) + runtime asserted, 3/3. Minor(final): guard relies on hand-written literals matching real RU5 consumer types (inherent to §5 approach).
=== CUTOVER STATUS (17/21 done: T1–T16 + T19) ===
T19: complete (commit 6777d02f, review Approved/clean). FE sim-state store + 5 shadcn components + FE jsonPointer (byte-identical to api RFC-6901) + 15 i18n keys (welcome.* + all existing preserved). applyStatePatch immutable via setByJsonPointer. Minor→T20: simulation.toolbar.openPanel unused key (wire in T20); components NOT yet mounted + SSE parser (api.ts) doesn't yet recognize simulation_state_patch/snapshot → T20 mounts + wires + collapses the 2 FE streams into one.
IN PROGRESS: T17 (backend sim caps [real in-process mcpPool] + ExecutionEvent→sim-SSE bridge).
  ⚠ WATCH T17: progress mentioned a '__probe.ts' scratch file — when committing, stage ONLY simulationCapabilities.ts + executionEventBridge.ts + their tests; verify NO stray __probe.ts left in tree.
T17: complete (commit 5953fe3d, review Approved — caps + mcpPool REAL by identity, bridge covers all 14 ExecutionEvent kinds, 32/32 100% cov). Both deviations sound (local throwaway union ext AgentSimEvent/WorkflowSimEvent; createServiceClient matches simulateHandler pattern).
  🚩🚩 T18 HARD DEPENDENCIES (from T17 review — CRITICAL):
  (1) The bridge is a PARTIAL SSE producer BY DESIGN — nulls the CONTENT events (assistant_message/tool_call/tool_result/child_dispatched/node_entered/node_exited/node_error). T18 MUST keep the EXISTING per-step/node callbacks (onStepProcessed/onNodeProcessed) emitting the legacy content SSE (messages/tools/dispatched) with rich parent metadata — the bridge only ADDS the NEW events (simulation_state_patch/snapshot, child_finished, child_awaiting_input, error, finished). Wiring the bridge as the SOLE SSE path → FE renders NOTHING for messages/tools.
  (2) T18 MUST widen writeSSE (simulate.ts:18, currently only SimulationEvent) to the bridge's extended unions (+SimulationStatePatch/Snapshot, workflow +ChildFinished) or forwarded events won't typecheck.
  (3) Resolve T15 carry-forwards: real child execution IDs (not child-${depth}), orgId plumbing (not ''), selectStepMachine call-site + ADOPT overloads (T14 rec).
T20: committed ab834d7f (parent = user's 'check' commit f4138537 [layout.tsx+icons] — my work on top, no conflict) — COMPLETE (commit ab834d7f + fix 010c992d; review Approved-with-fixes — runtime correct on all 6 checks; fix added the 2 brief-mandated tests [sendSim asserts one streamSimulation + correct appType] + removed dead streamAgentSimulation). Unified streamSimulation (POST /api/simulate, both engines); api.ts SSE parser routes simulation_state_snapshot/_patch → T19 store adoptSnapshot/applyStatePatch; SimulationStatePanel mounted via SimulationStateButton in panel header. Content events unchanged (additive).
  🚩 DEFERRED (host/data decision — NOT mounted): TestingPresetsPopover (needs tenant/preset data) + McpSideEffectBadge (needs per-tool providerType). T19 built them; a follow-up must find the host + data. → FLAGGED TO USER.
T18: complete (commit df4757c1, review Approved task-scoped). Unified appType-routed handler; BOTH engines onto executeTurn; content events STILL emitted live via existing per-step/node callbacks (bridge ADDITIVE-only over output.events); writeSSE widened; real orgId via buildSimServices.resolveChildConfig closure; child-${depth} placeholder never reaches FE (sim identity from rich child_dispatched callback); api selectStepMachine overloads. Backend 815 pass/4 skip, api 614 pass, both typecheck 0.
  🚩🚩 ISSUE #1 = MUST-FIX BEFORE MERGE (Important, near-Critical — the headline RU3 finding): the child_finished REORDERING is an FE-COMPOSITION INCOMPATIBILITY, not mere ordering. New backend = INLINE single-stream (syncRecurseStrategy runs children inline; bridged child_finished drains AFTER executeTurn returns, so parent-resume content precedes child_finished). But the CURRENT FE (simulationSendHelpers / compositionMachine / useAutoResumeParent) assumes a MULTI-STREAM model: parent STOPS at dispatch and the FE opens a FRESH stream on child_finished. Against an inline single stream this implies double child execution + double resume + content misrouted to child level. → RECONCILE the FE composition model to consume inline single-stream (parent does NOT stop at dispatch; no fresh stream on child_finished); VALIDATE END-TO-END before merge. Not T18-blocking only because the bridge is throwaway (RU5 replaces it) + the RU merge strategy mandates end-to-end user testing pre-merge.
  Other T18 follow-ups (before merge): #2 shallow tests — the unified test only asserts workflow routing + simulation_complete + agent zod-reject; NO agent happy-path / live-content / child-ordering / state-forwarding test. #3 writeBridged uses raw res.write (no flush) — state/child events don't flush incrementally. #4 DEAD CODE: simulateAgentHandler.ts no longer imported by server.ts + simulationOrchestrator.runSimulationOrchestration/handleDispatch/runChild now dead (only buildLoopConfig/buildLoopCallbacks reused) — remove or annotate retained-for-RU5. #5 mcpPoolStub THROWS on invoke (latent landmine — make a safe no-op returning empty).
T21 GATE RAN (report: .superpowers/sdd/ru3-task-21-gate-report.md): typecheck api/backend/web all 0; tests api 614 / backend 815+4skip / shared-store-services 47 GREEN; lint all 0. WEB RED: 2 fails = 3 missing cursor tool-catalog i18n keys (kv_store.list_keys, kv_store.search, rag.search) — LATENT RU1 offset→cursor regression (npm run check skips tests so never caught). → FIXING NOW (P0 translation). Seams 4/5 wired; DEVIATION: live driver passes THROWING mcpPoolStub (simulationDriverHelpers.ts:86) not T17's real pool — real MCP runs via engine closures (stub never invoked on sim path).
=== CONSOLIDATED MUST-FIX-BEFORE-MERGE (whole RU3 branch) ===
  P0: FIXED (commit eb091fa6) — web suite 157 tests, 0 fail. *** T21 GATE NOW GREEN: all packages typecheck 0 + lint 0 + tests pass (api 614 / backend 815+4skip / web 157 / shared-store-services 47). RU3 COMPLETE 21/21, ~24 commits, every task adversarially reviewed. Branch feat/migration NOT yet merged — pending the must-fix list below + RU4-6 + end-to-end user test (per RU merge strategy). ***
  P1 (headline): Issue #1 FE composition — useSimulationSend.ts:144 still MULTI-STREAM (parent stops at dispatch + fresh stream on resume) vs new INLINE single-stream backend → double child exec/resume. Reconcile FE + validate E2E (child-agent dispatch sim) before merge.
  P2 (BE cleanup): mcpPoolStub throws → safe no-op; dead code simulateAgentHandler.ts + simulationOrchestrator.runSimulationOrchestration/handleDispatch/runChild; writeBridged no-flush; shallow unified-handler tests (no agent happy-path/content/ordering/state test).
  RU4-latent (sim-masked, fix when RU4 durable path lands): childExecutionId collision executeTurn.ts:40; orgId:'' executeTurn.ts:64.
  Deferred FE (data decision): TestingPresetsPopover (tenant/preset data) + McpSideEffectBadge (per-tool providerType) built but unmounted.
NEXT: T18 (BOTH sim handlers onto executeTurn behind ONE handler, BE routes by appType; resolve T15 carry-forwards: real child IDs, orgId plumbing, selectStepMachine call-site + adopt overloads) ←T17. T20 (mount T19 components + SSE parser recognizes simulation_state_patch/snapshot + collapse 2 FE stream fns into one + wire openPanel + toolbar) ←T19. T21 full gate (whole-branch check).
IN PROGRESS: T4 (capability contracts ← T3, uses real McpInvoker + adds re-export), T8 (simStateStore ← T7). NEXT: T6←T4 (sim caps). Then MIDDLE layer: T9 ProviderCtx→env-union [BREAKING, updates all builtins+construction sites], T10 depth guard, T11 childDispatch, T12 simulatedNoop seam (per-builtin incl web), T13 resolveChildConfig adapter → T14 StepMachine (Agent+Workflow adapters + selectStepMachine) → T15 executeTurn → T16 barrel+completeness → T17-20 sim caps wiring + bridge + BOTH sim handlers onto executeTurn + FE collapse → T21 full gate.
RU2 Task 12: COMPLETE (commit a32f4e4c, review Approved/clean — deferral verified legitimate, genuinely blocked by api seam). simulateMcpInvoker.ts (buildSimulationMcpInvoker adapter) routes sim MCP through in-process pool; FULL registry threading (composeRegistry/buildMcpProvider, retire guardedCreateSession) BLOCKED by api seam gap (no mcpPool forward, McpInvoker not exported) → deferred RU3/RU4 per spec §2 (same boundary as RU2-T10 ProviderCtx injection). 1/1 + existing 2/2.
  ⚠️ CRITICAL CARRY-FORWARD → RU3/RU4 (the pool is NOT yet wired into the live sim OR prod path — buildSimulationMcpInvoker is DEAD until then, reachable only from its test): must (a) export McpInvoker/McpInvokeArgs from @daviddh/llm-graph-runner root; (b) add an mcpPool forward to composeRegistry/ComposeRegistryArgs + freezeProviders; (c) wire buildSimulationMcpInvoker (sim) + a prod invoker into the registry/ProviderCtx; (d) then retire guardedCreateSession/createMcpSession (lifecycle.ts deletion = RU6). Minor: SimInvokerArgs.orgId carried-unused (anticipates RU3/RU4 connect-binding).

*** RU2 (MCP CONNECTION POOL) COMPLETE: 12/12 tasks + membership comma-parse fix + RU2-T2/T8/T10 fix-loops — all reviewed clean. *** NOTE: user committed 240c3342 "updated build command" (root package.json, 1 line) — their change, leave it.

## RU2 — MCP connection pool (12 tasks)
Task 1: complete (commit e004ea70, parent 248894c8, review Approved/clean). Pool scaffold; full check EXIT 0. Implementer added a rejection-clears-`connecting` branch (fixes genuine latent bug: a permanently-rejected shared promise would pin a key forever) + 4th test; deferred DEFAULT_MAX_* consts to T2 (ConnectionPoolOptions signature kept stable).
  Minor (final review): closeHandle exported but unused in T1 (wired for later tasks); entries() returns the live internal Map by reference (in-spec mandated, but breaks encapsulation — a later task could mutate pool state bypassing refcount invariants).
Task 2: complete (commits 244b6722 + fix 2c354c99, parent chain from 5590019e, review Approved after 1 fix loop). Eviction (TTL+LRU, stdio tighter, default 200 vs 2000) + keepalive (stoppable, fake-timer tested) + lazy reconnect-on-borrow — all correct, borrowed-entry eviction triple-guarded. FIX: connect-failure refcount leak — borrowWith now unwinds (unwindFailedBorrow) decrement+delete husk on rejecting connect; verified correct even for concurrent shared-connecting-promise unwind. 16 pool tests green.
  Carry-forward to T3: connect-failure COOLDOWN (connectFailedUntil) still deferred to T3 (brief steps never defined it). Minor (final): closeEntry/connecting close-vs-reconnect race latent (not reachable now); newEntry hardcodes Date.now() for initial lastUsed (re-stamped on borrow); KeepaliveOptions.now dropped (unused). NOTE: this jest version uses --testPathPatterns (plural), not --testPathPattern.
  Lint fixup commit 4c36af9c (see LINT GATE lesson above).
Task 3: complete (commit 0b5ac5fa, parent 4c36af9c, review Approved/clean). connectEntry.ts: egress guard BEFORE connect (SSRF-gated, test-locked) + OAuth resolveAccessToken(orgId+libraryItemId, NOT tenantId)+apply Bearer header + 401→reconnect-once (bounded, no loop) + transport-close-on-failure. Type-guard mock sound.
  ⚠️ COOLDOWN CARRY-FORWARD: connectFailedUntil STILL unwired — connectEntry is a stateless orchestrator with no entry-state access, so cooldown (set connectFailedUntil on failure + reject-fast while cooling) belongs in the connectionPool BORROW path where connectEntry is wired in as the connect callback (likely T8 /internal/mcp/invoke or the pool wiring task). MUST be addressed there.
  Minor (final): add comment "401 here = expired OAuth bearer, not just session reattach"; applyAuthHeader uses lowercase 'authorization' key (fine).
Task 4: complete (commit 0455d783, parent 5f3a9a78, review Approved/clean). Both deviations verified correct (& U32 removal fixes hash re-signing; non-null replacement mandatory). Real consistent hashing (sorted ring + clockwise wrap), reshuffle test non-vacuous. Minor (final): firstClockwise unreachable null branch (harmless type-widening). ring.ts: consistent-hash ring (~150 vnodes/machine), ownerFor(poolKey) clockwise. Deviations: dropped brief's & U32 mask (claim: re-signs unsigned hash, breaks comparison); destructuring vs banned non-null assertion. 4/4, lint 0.
Task 5: complete (commit 512b0740, parent 0455d783, review Approved/clean). membership.ts: resolveTxt vms.<app>.internal → {machineId,region}[], ~3s cache (synchronous store() avoids require-atomic-updates race), local id from FLY_MACHINE_ID, defensive TXT parse (string[][], first token=machineId, skips empties). Added FLY_APP_NAME+FLY_MACHINE_ID to backend .env.example. 7/7, lint 0.
  Carry-forward → T6 routing: DNS-failure resilience unhandled (resolveTxt reject propagates; cache stays intact, retries next call — reasonable but no serve-stale-on-error). T6 decides whether a transient DNS blip should take down routing; add a reject-path test. TXT format `<machine_id> <region>` is per Fly docs. ⚠️ CONFIRMED BUG (web research 2026-06-27, fly.io docs): `vms.<app>.internal` TXT is a COMMA-SEPARATED list of '<machine_id> <region>' entries. Current membership.ts parser splits resolveTxt records + takes first whitespace token PER RECORD — if Fly returns one comma-joined record it reads ONLY THE FIRST machine (ring sees 1 node → never shards). FIX: join chunks → split on commas → first token of each entry = machine id (+ test with a comma-joined record). FLY_MACHINE_ID for localId CONFIRMED correct. all.vms.<app>.internal includes stopped; vms. = started only. FIXED (commit 56e9bd77, review Approved — parseRecord/parseEntry: join chunks → split commas → tokenize; all 6 edge cases traced: comma-join/multi-record/multi-chunk-join-before-split/empty-skip/no-drop). RU2-T5 follow-up done.
Task 6: complete (commit d43a388a, parent 240c3342, review Approved/clean). routing.ts: decideRoute (owner==self/empty→local; else→replay w/ owner machineId); forwardWithReroute (dead owner→rebuild ring minus owner→next; next==self/null→HandleLocallyError) — provably single re-route hop, no loop. resolveMembers: membership.list() reject → local-only fallback (the T5 DNS carry-forward). 7/7. fly-replay HEADER emission is the CALLER's job (routing is transport-agnostic, returns {kind:'replay',machineId}).
  Minor (final): bare catch{} re-routes on ANY forward error (not just connection failure) + swallows original error w/o log; no test for second-consecutive-dead-owner propagation.
Task 7: complete (commit 6a9fa49f, parent 7169916e [user's "better ui"], review Approved/clean). resolveBinding.ts: SERVER-SIDE trust boundary — orgId from agent row (never caller), server config from agent's own published graph (s.id===mcpBindingId), tenant override double-scoped (DB .eq('agent_id') + in-mem server_id&&tenant_id predicate), env decrypted under DB-derived org_id. Missing agent/server → BindingNotFoundError. Cross-tenant-leak guard tested (non-vacuous). 5/5. Security verified concretely by reviewer.
  Minor (final): one allowed `as` cast on graph.mcpServers (report wrongly claimed none); buildResolveBindingDeps DB-wiring glue uncovered (logic fully tested via injected deps).
Task 8: committed abdd6ea6 (parent cf43ebbf) — review NEEDS-FIXES (Important borrowLive leak) → fix DONE+Approved — RU2-T8 COMPLETE (commit abdd6ea6 + fix ea729089, 1 fix loop). Minor(final): release leaves a reclaimable {handle:null,borrows:0} husk until eviction sweeps (NOT a leak — eviction gates on borrows===0). SECURITY CLEARED: unauthed route is master-key gated (requireInternalAuth on all /internal/*, fails closed; SYSTEM_PUBLIC_UNAUTHED only exempts JWT). uncertain_outcome boundary correct (exactly at callTool). LEAK: borrowLive calls validateOrReconnect AFTER a successful borrow (refcount=1); if the health-probe reconnect connect() throws, returns transport error WITHOUT release → un-evictable husk borrows=1. Latent now (isHealthy=true) but live with a real ping probe. Fix=release-on-throw in borrowLive + test. Minor(final): client-pre-send callTool throw over-labeled uncertain; reconnect bypasses connecting-promise dedup. mcpInvoke.ts (POST /internal/mcp/invoke) + poolService.ts: resolveBinding→routing→egress re-validate→borrow(connectEntry)→callTool→release. uncertain_outcome ONLY when callTool throws (request on wire); pre-exec failures (binding/egress/borrow/reconnect) transparent. Route added to server.ts SYSTEM_PUBLIC_UNAUTHED (reviewer verifying master-key gating — SECURITY). 6/6.
  COOLDOWN DECISION NEEDED: connectFailedUntil still unwired — bounced T2→T3→T8, NO brief defines policy (duration/scope/reject-shape). Either implement with chosen defaults OR remove the unused field. Raise at RU2 end.
Task 9: complete (commit f36388c0, parent 7e72e9e1, review Approved/clean). mcpPreflight.ts: warm = resolveBinding→egress→borrow(connectEntry)→finally release, NO callTool → 200 ok:true / 400 ok:false. No-leak verified on all 3 paths (success/connect-fail/binding-egress). Deviations sound (tenantId in body like invoke; orgId closure cache — both DB-trust-boundary-safe). Master-key gate genuine. Mounts /mcp/preflight + allowlist; other routes untouched. 7/7.
Task 10: committed 021a6a75 (parent 38ecbf36) — review NEEDS-FIXES (Important) → fix DONE — RU2-T10 COMPLETE (commits 021a6a75 + dac32862; 1-line tenantId-in-body fix, RED→GREEN tested, controller-verified — too trivial for a full re-review). poolClient.ts (createMcpPoolClient: poolKey + x-mcp-poolkey + x-master-key → POST /internal/mcp/invoke; transport/uncertain/binding → distinct McpPoolError) + ADDITIVE buildMcpProvider seam (optional mcpPool, withSession default preserved, both branches tested). poolKey byte-identical to backend (VERIFIED). FIX: request body OMITS tenantId which backend BodySchema requires (z.string().min(1)) + uses to rebuild the routing poolKey server-side (handler ignores the header for that) → 400 binding every call. One-line: add tenantId:a.tenantId to body + test. Latent (not prod-wired). Minor(final): poolKeyShared.ts DUPLICATES backend poolKey — unify via shared types in RU3/RU6 (divergence risk).
Task 11: complete (commit caba2832, parent dac32862, controller-applied inline — config-only, no subagent). fly.toml [[http_service.http_options.replay_cache]]: path_prefix=/internal/mcp/, ttl_seconds=60, type=header, name=x-mcp-poolkey → fly-replay routing cached 60s so repeat tool calls for a poolKey hit the same warm machine. flyToml.test.ts asserts the block. 1/1.

---
## ARCHIVED: triggers run (feat/data-tools, MERGED into main via #29) — historical, do not re-dispatch
Triggers A1–A2,B1–B2,C1–C5,D1–D4 all complete + final review + fix-wave. Feature merge-ready and merged.

---

# Decision Node Option Rows (DN) SDD progress

Branch: feat/migration (in-place, shared with user's parallel commits).
Plan: docs/superpowers/plans/2026-07-13-decision-node-option-rows.md
Model: ALL subagents = opus (user standing rule).
Standing rules: same as RU section above (never discard un-authored changes;
explicit pathspec commits; derive each task's review BASE from actual git parent;
lint gate 0 errors on every implementer AND fix dispatch).

## Tasks
- DN-Task 1: shared nodeKind util + normalizeRowModeHandles — NOT STARTED
- DN-Task 2: normalize handles at ReactFlow boundaries — NOT STARTED
- DN-Task 3: render option rows + per-row handles + i18n — NOT STARTED
- DN-Task 4: add-option flow + default precondition — NOT STARTED
- DN-Task 5: per-row delete via confirm modal — NOT STARTED
- DN-Task 6: layout height for row-mode nodes — NOT STARTED

### DN progress log
- DN-Task 1: COMPLETE (commit a1f7773d, review clean — Spec ✅, quality Approved).
  Minors for final triage: (a) nodeKind.test.ts double-cast tool fixture (from brief, test-only);
  (b) nodeKind.test.ts one line >110 Prettier width — run `npm run format` on feature files before finishing.
  Lesson: this Jest version needs `--testPathPatterns` (plural), not `--testPathPattern`.
- DN-Task 2: COMPLETE (commit 87b4c5ea, review clean — Spec ✅, quality Approved).
  Minor for final triage: DebugCanvas toRFEdges lacks explicit return type (inference correct).
- DN-Task 3: COMPLETE (commit 41d13379, review clean — Spec ✅, quality Approved).
  Minor for final triage: NodeOptionRow line-clamp-1! important modifier (matches NodeBody convention).
- DN-REGRESSION FIX (commit d6cc9402): start node (INITIAL_STEP) edges vanished — user_said edges made
  getNodeKind classify it user_routing → normalize rewrote sourceHandle to edge.id (StartNode has no such
  handle). Fixed: exclude START_NODE_ID in getRowModeSourceIds + regression test. 8/8 nodeKind tests pass.
- DN-Task 4: COMPLETE (commit 301498aa, review clean — Spec ✅, quality Approved).
  Minor for final triage: option preconditions carry cosmetic description:undefined (harmless).
- DN-Task 5: COMPLETE (commit b62c7bc6, review clean — Spec ✅, quality Approved, no issues).
- DN-Task 6: COMPLETE (commit 1f1c92ae, review clean — Spec ✅, quality Approved).
  Minor for final triage: nodeDimensions test uses relative-only assertions (could pin exact 300/220).
- ALL DN TASKS COMPLETE. Feature commits: a1f7773d 87b4c5ea 41d13379 d6cc9402(fix) 301498aa b62c7bc6 1f1c92ae.

# Keyboard-undo SDD progress (2026-07-15)

Plan: docs/superpowers/plans/2026-07-15-keyboard-undo.md (9 tasks)
Spec: docs/superpowers/specs/2026-07-15-keyboard-undo-design.md
Branch: feat/migration. Model: ALL subagents = opus (user standing rule).
Standing rules from RU section apply (explicit staging, lint gate, user commits in parallel — derive BASE from git log).
Base: 8b527fad

Task 1: complete (commit ffdf5fe8, review clean, spec+quality approved)
Task 2: complete (commit 5812eb70, review clean, spec+quality approved)
  Minor (final-review triage): task-2-report.md misdescribes edgeKey separator as a space (code correctly uses U+0000 NUL); intra-group insert/update interleave in upserts (harmless, disclosed).
Task 3: complete (commit 454aa075, review clean, spec+quality approved)
  Contract note for later tasks: dispatch signature is dispatch(id: string, params: unknown, opts?) (no generic) — call shapes unchanged; register<P> overload keeps definer typing.
Task 4: complete pending test-strengthen fix (commit 0a818c2d, spec+quality approved)
  Fix in flight: preState-ordering test cannot detect order flip (static state object) — strengthening so a flipped getState/onLiveChange order fails.
  Task 7 dispatch note: panels must remount (key by nodeId/edge key) per selection, else CommittedFieldCore.lastCommitted retains prior node's value.
Task 4: complete (commits 0a818c2d + f815f2c7 test-strengthen w/ mutation-check evidence, review clean)
Task 5: complete (commit 12a08754, review clean, spec+quality approved)
  jest.config.js: .tsx now in extensionsToTreatAsEsm (needed for rhh ESM); component tests use .test.ts + createElement (testMatch globs only .ts).
  Task 8 note: enabled(ctx) gating test currently vacuous (no Escape row yet) — Task 8 must add a real gated-row assertion.
Task 6: complete (commit 5b7759cd, review clean on all 8 failure modes, spec+quality approved)
  Contract: dispatch ids as planned; multi-arg creators bundled (IfElseParams/LoopParams); edge.updateProps data optional matching buildUpdateEdgeOp.
Task 7: implemented (commit 3452d8d5), HIGH fix in flight: node.commitProps one-render-stale read on synchronous discrete updates — extending action with explicit updates payload (mirrors edge.updateProps). Reviewer gets combined range after fix.
GATE LESSON: eslint from repo root resolves a config WITHOUT react-hooks rules. Binding gate for web files is `npm run lint -w packages/web`. All remaining task/fix dispatches must use the package-level lint. Fix in flight for 4 react-hooks/refs errors (useCommittedField, useEditorIntegration).
Task 7: complete (commits 3452d8d5 + 70864a7c stale-fix + 7de29fcd refs-fix, review clean)
  Minor (final-review triage): pushUpdateNode/pushUpdateEdge exports now dead (zero callers) — candidate deletion; task-7-report usage claim inaccurate.
Task 8: complete (commit 4ff8185e, review clean, zero findings, spec+quality approved)
Task 9: complete (commit adcf2925 coverage table test; suites 289 green, lint 0 errors, typecheck clean, all new modules >=95% lines - useEditorIntegration 100%)
Final whole-branch review: NEEDS FIXES. I-1 lastCommitted resync (undo-then-retype silent loss); I-2 panel delete/rename/edge-delete/type-change bypass registry (spec includes them in undo scope; plan caller list missed them); Minor: delete dead pushUpdateNode/pushUpdateEdge. One fixer dispatched with full list. Accepted: edgeKey prose, upsert interleave.
Final-review fixes: complete (169d18f6 I-1 resync, 37a888df I-2 panel actions, 892b0d6a dead exports). Re-review verdict: READY TO MERGE. Gates at HEAD: 297 tests green, lint 0 errors, typecheck clean.
PROJECT COMPLETE: keyboard-undo (16 commits, 8b527fad..892b0d6a on feat/migration). Pending: user manual dev-server pass (checklist in final summary).
